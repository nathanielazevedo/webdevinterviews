# Backend-specific ignores (root .gitignore handles common patterns)

# Environment variables (backend-specific)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Test coverage (backend-specific)
coverage/

# Runtime data (backend-specific)
pids
*.pid
*.seed
*.pid.lock