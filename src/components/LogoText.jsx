import Typography from '@mui/material/Typography'

const LogoText = () => {
  return (
    <Typography variant='subtitle' noWrap component='div' fontWeight={'bold'}>
      &lt; &nbsp; FLOW STATE CODE &nbsp; / &gt;
    </Typography>
  )
}

export default LogoText
